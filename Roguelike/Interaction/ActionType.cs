namespace Roguelike.Interaction
{
    /// <summary>
    /// Types of possible player actions.
    /// </summary>
    public enum ActionType
    {
        Exit, ApplyHealth, ApplyForce, ApplyAll, TakeOffHealth,
        TakeOffForce, TakeOffAll, ApplyExperience, TakeOffExperience
    }
}